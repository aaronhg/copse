node_modules/
.DS_Store
dist/
.mcp.json
# harvested third-party game data + fetched bundles — NEVER commit (public repo; contains
# operator/game/host identifiers + copyrighted assets). Covers harvest/, harvest-headed/, etc.
harvest*/
# local Cocos engine source checkouts (git clone per version → reference/cocos/<ver>) used to
# validate copse's internal-field assumptions + build real-engine test fixtures. Third-party,
# hundreds of MB each — a local reference, never vendored into copse.
reference/
# this machine's framework adapters (PureMVC etc.) — auto-loaded by the driver, injected into the
# game. Game-/vendor-specific + not every game uses a framework, so it stays LOCAL: core is
# framework-agnostic. The git-tracked copse.frameworks.example.mjs documents the format.
copse.frameworks.mjs
